logging:
  level:
    com.example.profiler.demo: DEBUG
management:
  endpoints:
    web:
      exposure:
        include: health
server:
  port: 8080
spring:
  application:
    name: profiler-demo
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
